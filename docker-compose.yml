version: '2.4'

networks:
  backend:
    driver: bridge

services:
  workspace:
    restart: unless-stopped
    build: ./docker/cli
    hostname: workspace
    working_dir: /var/www/code
    volumes:
      - ./:/var/www/code
    environment:
      TZ: ${TZ:-US/Central}
    tty: true
    networks:
      - backend
